# =============================================================================
# BucketCode — Database Environment (packages/db)
# =============================================================================
# Copy this file to `.env` and replace with your actual Neon connection string.
#   cp .env.example .env
# =============================================================================

# Neon Postgres 17 — Single URL config (pooled connection via Neon proxy)
DATABASE_URL="postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/bucketcode?sslmode=require"
